#include <mruby.h>
#include <mruby/irep.h>

/* Placeholder — regenerated at build time by mrbgem.rake from the
   script set via `spec.hypha_main` or HYPHA_SCRIPT.
   This stub keeps the bin-gem source glob happy on a fresh checkout. */

const uint8_t hypha_main[] = { 0 };
